---
description: App Router 新页面 — 目录、步骤、组件与动画约定（Next.js 15+）
globs:
  - src/app/**/*.tsx
  - src/components/**/*.tsx
alwaysApply: false
---

# 新页面（App Router）— 本仓库约定

与通用 NextSaaS 模板不同：本项目的 `Navbar` / `Footer` / `<main>` 已在根 `layout.tsx` 中包裹，**页面文件内不要再套一层** `NavbarOne` + `main` + `FooterOne`。

## 项目目录说明

```text
src/
  app/                      # App Router
    layout.tsx              # 根布局（含 Navbar、main、Footer）
    page.tsx                  # 首页 /
    globals.css
    <route-name>/             # 静态路由，文件夹 kebab-case
      page.tsx
    <segment>/                # 动态路由示例
      [slug]/
        page.tsx
  components/
    <route-name>/             # 与该路由对应的页面区块组件（与 app 路由语义对齐）
    shared/                   # 全站共用（layout、navbar、footer、cta…）
    animation/                # RevealAnimation、TextReveal 等
    home/
    about/
    …
  context/
  data/
  hooks/
  interface/
  utils/                      # 含 generateMetaData 等
```

新建功能页时：**先在 `src/app/<route>/page.tsx` 注册路由**，再在 `src/components/<route>/` 下增加区块组件（与 `about/`、`home/` 相同思路）。

## 创建页面（步骤）

1. **建目录与 `page.tsx`**：在 `src/app/` 下创建 `your-route/page.tsx`（或 `your-route/[slug]/page.tsx`）。
2. **写 `metadata`**：从 `@/src/utils/generateMetaData` 引入 `generateMetadata`，`export const metadata` 中先 `...generateMetadata()`，再覆盖 `title`、`description`（必要时 `openGraph`）。
3. **写页面主体**：默认导出组件；返回 `<>...</>` 或一组 section 组件，**不要**再包一层 `<main>`。
4. **建页面组件目录**：`src/components/your-route/`，按区块拆文件（如 `hero.tsx`、`features.tsx`），在 `page.tsx` 中按需组合。

## `page.tsx` 模板

```tsx
import YourHero from "@/src/components/your-route/hero"
import { generateMetadata } from "@/src/utils/generateMetaData"
import { Metadata } from "next"

export const metadata: Metadata = {
  ...generateMetadata(),
  title: "Your title - AI Solutions || AIClawers",
  description: "SEO 用简短描述",
}

const page = () => {
  return (
    <>
      <YourHero />
    </>
  )
}

export default page
```

## 页面组件模板

- 路径：`@/src/components/your-route/...`（**不要**使用 `@/components/...`）。
- 文件名：**kebab-case**（如 `why-us.tsx`）；文件内导出组件名用 **PascalCase**（如 `WhyUs`）。
- 区块使用 `main-container` 与现有页面的 typography / spacing 类名保持一致。

```tsx
import RevealAnimation from "@/src/components/animation/reveal-animation"

const YourHero = () => {
  return (
    <section className="pt-44 pb-14 md:pb-20">
      <div className="main-container">
        <RevealAnimation delay={0.1} instant>
          <h1 className="font-sora text-center text-white/90">Your title</h1>
        </RevealAnimation>
      </div>
    </section>
  )
}

export default YourHero
```

## 动画（RevealAnimation）

```tsx
import RevealAnimation from "@/src/components/animation/reveal-animation"

<RevealAnimation delay={0.1} instant>
  <div>内容</div>
</RevealAnimation>

<RevealAnimation delay={0.2} direction="up" offset={50}>
  <div>内容</div>
</RevealAnimation>

<RevealAnimation delay={0.3} useSpring duration={2}>
  <div>内容</div>
</RevealAnimation>
```

标题类动效可配合 `@/src/components/animation/text-reveal-animation` 的 `TextReveal`，与 `about`、`home` 保持一致。

## 最佳实践

- 路由与组件目录：**kebab-case**。
- 每个公开路由提供 **Metadata**。
- 有 props 的组件用 **TypeScript interface** 声明 props。
- 需要调试名时可为组件设置 **`displayName`**（非强制，与现有代码风格一致即可）。
- 避免无意义的包裹：能用 `<>...</>` 就不用多余 `div`。
- 大页面按区块拆到同目录多个文件，**单一职责**。
