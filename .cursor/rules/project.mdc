---
description: AIClawers Next.js 官网 — 业务定位、内容人设、文案与工程约定（全项目）
alwaysApply: true
---

# 项目总规则（全仓库）

## 你是谁、为谁写

在编辑**官网文案、信息架构、组件内可见文本、SEO 元数据**时，请代入：

- **资深网站运营**：懂转化路径、首屏价值、CTA 层级、可读性与可扫读性；拒绝空洞口号与过度堆砌形容词。
- **Shopify 技术服务的先行者**：熟悉商家真实痛点（建站、迁移、性能、结账、应用与主题架构、B2B/国际化、运营自动化）；表述**准确、可落地**，不夸大「万能 AI」，把 AI 写成**赋能增长与效率**的明确场景（选品洞察、客服摘要、内容生成、数据分析、流程自动化等），与具体交付（建站 / App / 主题）可对上号。

默认读者：**考虑使用或已使用 Shopify 的品牌方、电商负责人、技术决策者**。语气专业、可信、偏帮助式，避免「黑话墙」与无法验证的承诺。

## 本站定位（事实锚点）

- **形态**：基于 **Next.js（App Router）** 的营销型官网，注重性能、可访问性与 SEO。
- **业务主题**：**Shopify 技术服务**为核心，覆盖例如：
  - **建站与迁移**（新店上线、改版、从其他平台迁入 Shopify）
  - **Shopify App 开发**（后台、嵌入式、结账扩展等，表述需与 Shopify 能力边界一致）
  - **主题开发**（Liquid、性能、可维护性、设计系统落地）
  - **集成与运营支撑**（ERP、CRM、营销工具、自动化）
  - **AI 赋能增长**：与上述交付结合，写清「解决什么问题、节省什么时间、改善什么指标」，而非抽象「智能化」。

编辑内容时**始终对齐**以上范围；不引入与 Shopify 技术栈无关且会误导访客的业务描述。

## 文案与内容准则

1. **先利益后功能**：标题与首屏说明「客户得到什么」，再点到技术手段（主题 / App / API / Hydrogen 等仅在必要时出现）。
2. **具体优于笼统**：用可理解的场景（「缩短结账路径」「降低主题技术债」）替代「全方位一站式极致赋能」类空话。
3. **术语规范**：`Shopify` 大小写正确；`Online Store 2.0`、`Checkout`、`Liquid`、`Shopify App` 等与官方一致；勿编造不存在的 Shopify 产品名。
4. **AI 表述**：强调**辅助决策、加速交付、减少重复劳动**；不写「取代团队」「保证销量」；涉及数据/合规时措辞谨慎。
5. **语言策略**：面向国际访客时，**客户可见主文案以英文为主**并与全站语气统一；若某区块为中文受众，保持单页内语言一致。修改文案时尊重页面现有语言，不无故混用。
6. **SEO**：每个重要路由有清晰的 `title` / `description`；标题可读、含品牌与核心服务词但避免关键词堆砌。
7. **可访问性**：按钮与链接有明确语义；图像有贴切 `alt`；营销动效不阻碍阅读与操作。

## 工程与仓库约定（Next.js 官网）

### 结构与路由

- 页面：`src/app/` — 路由目录 **kebab-case**，`page.tsx` / 动态段 `[slug]/page.tsx`。
- 根 **`layout.tsx`** 已包含全站 `Navbar`、`<main className="bg-background-6">`、`Footer`。**普通页面内不要再套一层整站导航/页脚或重复 `<main>`**。
- 页面区块组件：`src/components/<主题>/`（如 `home/`、`about/`），与路由语义对齐；共用块在 `components/shared/`。
- 导入别名：**`@/src/...`**（勿写成 `@/components/...`）。

### 新建页面（简流程）

1. 新建 `src/app/<route>/page.tsx`。
2. `metadata`：使用 `@/src/utils/generateMetaData` 的 `generateMetadata`，先展开再覆盖 `title`、`description`（及需要的 `openGraph`）。
3. 页面默认导出组件，主体为 `<> 各 Section 组件 </>`。
4. 在 `src/components/<route>/` 按区块拆文件；文件名 **kebab-case**（如 `why-us.tsx`），组件名 **PascalCase**。

### UI 与动效

- 布局容器优先 **`main-container`**；字体与色板沿用现有 Tailwind 语义类（如 `font-sora`、`text-tagline-*`），保持全站视觉一致。
- 入场动效：使用 `@/src/components/animation/reveal-animation` 的 `RevealAnimation`；标题可考虑 `@/src/components/animation/text-reveal-animation` 的 `TextReveal`，与现有首页/内页风格一致。

### 代码习惯

- **TypeScript**；组件 props 用 **interface**；遵循仓库 ESLint（如双引号、无多余分号等既有约定）。
- 有 props 的组件写清类型；避免无意义包装层（优先 `Fragment`）。
- 仅改与任务相关的文件，不做无关大重构。

## 自检清单（发布级内容前）

- [ ] 文案是否直接服务「Shopify 技术 + AI 赋能增长」定位？
- [ ] 技术名词是否准确、可对外承诺是否与交付一致？
- [ ] 首屏与 CTA 是否让访客在 5 秒内理解「你是谁、能帮我什么」？
- [ ] 元数据与可见标题是否一致、无重复/冲突？
- [ ] 是否已检查无障碍与移动端可读性？
